export class GisDto {
    constructor(agentViewModel) {
        if (agentViewModel != null && agentViewModel != undefined) {
        }
    }
    name: string;
    id: number;
    code: string;
    parrentId: string;
    parrentTitle: string;
    typeId: string;
    TransTime: number;

}